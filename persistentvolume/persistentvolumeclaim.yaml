apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: task-pvc
spec:
  volumeName: task-pv
  storageClassName: local-storage
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
